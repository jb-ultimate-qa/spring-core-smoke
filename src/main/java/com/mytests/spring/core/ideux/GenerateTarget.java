package com.mytests.spring.core.ideux;

import org.springframework.stereotype.Service;

/**
 * Scratch target for the Spring. Core. IDE UX generate-action cases.
 * <p>
 * Generate actions insert fields, methods and whole classes. They must be
 * invoked HERE and nowhere else: every other class in this project is a source
 * anchor for a Spring. Core case, and generated members would shift the lines
 * those cases assert on.
 * <p>
 * Keep the anchor comment below - the cases place the caret on it.
 */
@Service
public class GenerateTarget {

  // generate here
}
